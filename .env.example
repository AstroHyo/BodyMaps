RUNPOD_ENDPOINT=
RUNPOD_ENDPOINT_KEY=
BLOB_READ_WRITE_TOKEN=
PASSWORD=
# Local development only. Ignored when NODE_ENV=production.
# DISABLE_PASSWORD=true
# Optional comma-separated extra HTTPS hosts allowed for CT download URLs.
# Vercel Blob public hosts are allowed by default.
# ALLOWED_CT_URL_HOSTS=.example.edu
# Optional worker download cap in bytes. Defaults to 32 MiB.
# MAX_CT_DOWNLOAD_BYTES=33554432
# NEXT_PUBLIC_ALLOW_SAMPLE_DATA=true
